{
  "name": "delta-synth",
  "version": "2.4.0",
  "type": "module",
  "imports": {
    "public/*": "./src/public/*",
    "backend/*": "./src/backend/*"
  },
  "devDependencies": {
    "@wix/cli": "^1.0.0",
    "@wix/eslint-plugin-cli": "^1.0.0",
    "eslint": "^8.25.0",
    "react": "16.14.0"
  },
  "scripts": {
    "postinstall": "wix sync-types",
    "dev": "wix dev",
    "lint": "eslint .",
    "test": "node --loader ./tests/loader.js tests/run-all-tests.js",
    "build": "echo Ready"
  }
}